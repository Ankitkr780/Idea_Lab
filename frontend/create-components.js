const fs = require('fs');
const path = require('path');

console.log('Creating component files...\n');

// Component files will be created in next step
console.log('Component creation script ready!');
console.log('This will be used to create all remaining components.');
